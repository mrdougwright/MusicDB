<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @artist.name %>
</p>

<p>
  <strong>Genre:</strong>
  <%= @artist.genre.name %>
</p>

Showing all Songs in the <%= @artist.name %> artist:
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= song.name %></li>
    <% end %>

<%= link_to 'Edit', edit_artist_path(@artist) %> |
<%= link_to 'Back', artists_path %>
